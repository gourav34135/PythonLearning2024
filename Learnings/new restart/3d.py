# import the module
from vpython import *

# the first text object
text(text = "SUDHANSHU",
	pos = vector(-5, 2, 0),
	height = 3,
	depth = 1,
	color = vector(0.5, 1, 1))

# the second text object
text(text = "TAKLA",
	pos = vector(1, -1, 5), 
	color = vector(0, 1, 0))
